import { VRScroll } from 'react-gonglu-design';

export default () => {
  return (
    <section style={{ width: '500px', height: '600px', position: 'relative' }}>
      <VRScroll />
    </section>
  );
};
